necessary to fill COMMON_FOREIGN_RPC_URL, ORACLE_VALIDATOR_ADDRESS, ORACLE_VALIDATOR_ADDRESS_PRIVATE_KEY and ORACLE_MEV_FOREIGN_FLASHBOTS_AUTH_SIGNING_KEY with your own values. Where ORACLE_VALIDATOR_ADDRESS and ORACLE_VALIDATOR_ADDRESS_PRIVATE_KEY contain the address and the private key of the account which will execute transactions (it must be prepaid with some small amount of ether);  ORACLE_MEV_FOREIGN_FLASHBOTS_AUTH_SIGNING_KEY is an unique 32 bytes long id written in hex.